{
  "name": "madcamp-loop",
  "private": true,
  "scripts": {
    "build:loop": "cd frontend && npm run build:loop",
    "build": "cd frontend && npm run build",
    "dev": "cd frontend && npm run dev"
  }
}
